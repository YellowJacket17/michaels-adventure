package asset.enumeration;

/**
 * This enum defines the two types of sounds (track and effect).
 */
public enum SoundType {

    /**
     * Background music that plays during gameplay (loops).
     */
    TRACK,

    /**
     * Quick soundbite that plays during an event/action (does not loop).
     */
    EFFECT
}
