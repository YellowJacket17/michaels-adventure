package combat;

/**
 * This enum defines the specific transition types to be used with an exiting combat transition.
 */
public enum ExitCombatTransitionType {

    /**
     * Simple fade to and from black, exiting combat normally and restoring all entities to their pre-combat states.
     */
    BASIC
}
