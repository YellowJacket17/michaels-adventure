package ui.enumeration;

/**
 * This enum defines possible primary menu states, with each determining which menu is rendered in the user interface.
 */
public enum PrimaryMenuState {
    INACTIVE,
    PARTY,
    INVENTORY,
    SETTINGS;
}
